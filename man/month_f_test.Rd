% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/month-f-test.R
\name{month_f_test}
\alias{month_f_test}
\title{Test Whether Month Predicts Measles Cases by Region}
\usage{
month_f_test(
  selected_regions,
  selected_months = 1:12,
  data = load_data()$cases_month
)
}
\arguments{
\item{selected_regions}{A character vector of WHO region codes. Options are
\code{"AFR"}, \code{"AMR"}, \code{"EMR"}, \code{"EUR"}, \code{"SEAR"}, and \code{"WPR"}.}

\item{selected_months}{A numeric vector of month numbers from 1 to 12.
Defaults to all months.}

\item{data}{A data frame containing monthly measles data. Defaults to
\code{load_data()$cases_month}.}
}
\value{
A gt table with ANOVA F-test results for each selected region.
}
\description{
Runs separate one-way ANOVA F-tests to determine whether month is a
significant predictor of measles cases within one or more selected WHO
regions. The function returns a formatted gt table with one row per region.
}
\examples{
month_f_test("AMR")
month_f_test(c("AMR", "AFR"))
month_f_test(c("AMR", "AFR"), selected_months = c(1, 2, 3))
}
